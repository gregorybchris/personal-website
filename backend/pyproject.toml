[project]
name = "chris"
version = "1.1.0"
description = "Personal website backend"
authors = [
  { name = "Chris Gregory", email = "christopher.b.gregory@gmail.com" },
]
requires-python = ">=3.13"
readme = "README.md"
license = { file = "LICENSE.txt" }
keywords = ["personal", "website", "python", "tools"]
classifiers = [
  "Intended Audience :: Developers",
  "Topic :: Software Development",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.13",
]
dependencies = [
  "click>=8.1.7,<9",
  "fastapi[standard]>=0.113.0,<0.114",
  "httpx>=0.27.2,<0.28",
  "jsonvl>=0.5.0,<0.6",
  "pandas>=2.2.2,<3",
  "pydantic>=2.9.0,<3",
  "python-frontmatter>=1.1.0,<2",
]

[project.urls]
Repository = "https://github.com/gregorybchris/personal-website"

[project.scripts]
cgme = "chris.cli.cli:main"

[dependency-groups]
dev = [
  "covcheck[toml]>=0.4.3,<0.5",
  "mypy>=1.11.2,<2",
  "pytest>=8.3.2,<9",
  "pytest-cov>=5.0.0,<6",
  "ruff>=0.6.4,<0.7",
  "semver>=3.0.2,<4",
]

[tool.hatch.build.targets.sdist]
include = ["chris"]

[tool.hatch.build.targets.wheel]
include = ["chris"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.covcheck]
branch = 0.0
line = 0.0

[tool.mypy]
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_defs = true
ignore_missing_imports = true

[tool.ruff]
line-length = 120
src = ["chris"]

[tool.ruff.lint]
ignore = ["D100", "D104", "FIX002", "TD003"]
select = [
  "A",
  "ARG",
  "B",
  "BLE",
  "C4",
  "E",
  "EM",
  "ERA",
  "F",
  "FIX",
  "G",
  "I",
  "ICN",
  "ISC",
  "LOG",
  "N",
  "PL",
  "PT",
  "PTH",
  "PLR",
  "RET",
  "RUF",
  "SIM",
  "SLF",
  "T20",
  "TCH",
  "TD",
  "TID",
  "W",
]

[tool.ruff.lint.pycodestyle]
max-line-length = 120

[tool.ruff.lint.extend-per-file-ignores]
"**/*.py" = ["G004", "EM101", "EM102", "TD002"]
"**/tests/**/*.py" = ["SLF", "PLR2004", "PT013"]
"chris/utilities/generate_uuid.py" = ["T201"]
"chris/validation/validate.py" = ["T201"]
